.DS_Store

# Secrets must never enter this repository. Nothing here is a build artifact —
# these are all files that could plausibly hold a credential.
.env
.env.*
*.env
secrets/
.pi/secrets/
**/secrets/
auth.json
*.pem
*.p12
*_rsa
*_ed25519
credentials.json
